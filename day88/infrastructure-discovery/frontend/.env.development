# WSL2 / Docker: bind to all interfaces so Windows browser can reach localhost
HOST=0.0.0.0
PORT=3001
# Allow connections from different host (e.g. Windows accessing WSL2)
DANGEROUSLY_DISABLE_HOST_CHECK=true
