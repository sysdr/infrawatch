import React, { useState, useEffect } from "react"; export default function Dashboard() { const [stats, setStats] = useState({}); useEffect(() => { fetch("/api/stats").then(res => res.json()).then(data => setStats(data)); }, []); return (<div><h2>Dashboard</h2><div style={{display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(200px, 1fr))", gap: "1rem"}}><div style={{background: "#f8f9fa", padding: "1rem", borderRadius: "8px"}}><h3>Total Tasks</h3><p style={{fontSize: "2rem", margin: 0}}>{stats.total_tasks || 0}</p></div><div style={{background: "#f8f9fa", padding: "1rem", borderRadius: "8px"}}><h3>Active Tasks</h3><p style={{fontSize: "2rem", margin: 0}}>{stats.active_tasks || 0}</p></div><div style={{background: "#f8f9fa", padding: "1rem", borderRadius: "8px"}}><h3>Executions</h3><p style={{fontSize: "2rem", margin: 0}}>{stats.total_executions || 0}</p></div><div style={{background: "#f8f9fa", padding: "1rem", borderRadius: "8px"}}><h3>Success Rate</h3><p style={{fontSize: "2rem", margin: 0}}>{Math.round(stats.success_rate || 0)}%</p></div></div></div>); }
