import React from 'react';
import { Box } from '@mui/material';

const Sidebar = () => (
  <Box component="nav" sx={{ width: 240, borderRight: '1px solid #e0e0e0', bgcolor: 'background.paper' }} />
);

export default Sidebar;
